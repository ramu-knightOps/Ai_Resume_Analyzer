[providers]
python = {}

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "python -m pip install -r App/requirements.txt"
]

[start]
cmd = "bash ./start.sh"
